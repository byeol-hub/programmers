# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
user_input = list(map(int, input().split()))
que = []

for i in range(user_input[0]):
	que_input = list(input().split())
	if que_input[0] == 'push':
		if len(que) < user_input[1]:
			que.append(que_input[1])
		else:
			print("Overflow")
	else:
		if len(que) == 0:
			print("Underflow")
		else:
			print(que[0])
			que.pop(0)